import Foundation
import Supabase

// MARK: - Supabase 配置模板
// 复制此文件为 SupabaseConfig.swift 并填入你的真实信息

let supabase = SupabaseClient(
    supabaseURL: URL(string: "https://your-project-url.supabase.co")!,
    supabaseKey: "your-anon-public-key"
)
